# dispatch-instance-followup-20260918 — 같은 arm을 인스턴스 2개씩 돌린 후속 실험 (동시성 4)

- 실행: 2026-09-18 05:24–06:53 KST. 파드 original-a/b, capability-a/b(서명 직접 디스패치) + client, 각 200문항 1회, 순서
  original-a → capability-a → original-b → capability-b, 800요청(전송 실패 1건: original-a). 동결 이미지·모델·질문은 09-16 리뷰 실험과 동일.
- attachment에 브랜치별 핸들러 원시 결과·법령 ID 목록·다음 단계 객체의 SHA-256 해시를 추가 기록.
- `followup-analysis.json`(`analyze_followup.py`): arm 쌍별 정확 집합 일치·재현율·짝 지연, 준비 인자가 같은 문항의 출력 해시 동일 여부.
- 결과: 같은 arm의 두 인스턴스 일치 86.4%(원본)·83.5%(서명)는 다른 arm 사이(83.0–87.0%)와 같다. 준비 인자가 같은 164–170문항 중
  핸들러 법령 목록이 같은 경우는 65–79%, 원시 결과가 바이트 단위로 같은 경우는 5–17%. 즉 검색 경로 자체가 인스턴스 간 비결정적이며
  arm 간 품질 격차는 디스패치 때문이 아니다. 지연은 인스턴스 간 −0.6~+0.4초, arm 간 서명이 0.2–1.1초 빠름(구간 모두 0 포함).
- 오케스트레이터 엔진에 완료되지 않는 요청 1건이 남아 있어 "대기 0, 실행 ≤1" 유휴 게이트로 실행(프로토콜·상태에 기록).
- 원시 질문·응답·앱 로그는 서버(`~/cnu-review-followup-20260917/`)에만 있다.
